8月19日记

下载文件：var loader:URLLoader = new URLLoader();//loader.dataFormat = URLLoaderDataFormat.TEXT;loader.addEventListener(Event.COMPLETE, _loadComplete);try{loader.load(new URLRequest("data\\soft.xml"));}catch (error:Error){JOptionPane.showMessageDialog("提示", "loader.load发生错误");}━━━━━━━━━━Jack━━━━━━━━━2012.8.19━━━━━━━━━━构建List：dataXML = new XML(event.target.data);var listArray:Array = new Array();for (var i:int = 0; i < 4; i++){var sonArray:Array = new Array();for each (var nameXML:XML in dataXML.List[i]..@name){sonArray.push(nameXML.toString());}listArray.push(sonArray);}vMain.getList0().setListData(listArray[0]);vMain.getList1().setListData(listArray[1]);vMain.getList2().setListData(listArray[2]);vMain.getList3().setListData(listArray[3]);vMain.getList0().addSelectionListener(_selectList0);vMain.getList1().addSelectionListener(_selectList1);vMain.getList2().addSelectionListener(_selectList2);vMain.getList3().addSelectionListener(_selectList3);━━━━━━━━━━Jack━━━━━━━━━2012.8.19━━━━━━━━━━弹出url：navigateToURL(new URLRequest(dataXML.List[0].soft[vMain.getList0().getSelectedIndex()].@url.toString()), "_blank");━━━━━━━━━━Jack━━━━━━━━━2012.8.20━━━━━━━━━━得到全路径，var abs : String = neko.FileSystem.fullPath( “../data” );不存在就创建if (!neko.FileSystem.exists( “../images” ))    neko.FileSystem.createDirectory( “../images” );显示目录下的文件和文件夹，并测试了一下结果，想显示这个数组，for循环要这样写：var contents : Array < String >  = sys.FileSystem.readDirectory( "../" );for (i in 0...contents.length){Lib.println(contents[i]);}━━━━━━━━━━Jack━━━━━━━━━2012.8.20━━━━━━━━━━没人翻译给我看，自己翻译吧： Okay, so you can create directories and delete them, but you might find it hard to manage directories if you don ’ t know what they contain. Thankfully, Neko provides a handy  readDirectory  function for listing the contents of a directory as an array of strings:好的，于是你能创建路径并删除它们，但你可能发现很难管理路径如果你不知道他们包含什么的话，庆幸的是，Neko提供了一个函数readDirectory来列出一个路径包含的内容，它返回的是一个字符数组。