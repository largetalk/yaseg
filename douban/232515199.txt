File文件操作

读文件：    var stream = sys.io.File.read( "../src/Main.hx", false );    var str ="";    while ( !stream.eof() )    {      try      {        Lib.println(stream.readLine());      }      catch ( err : Dynamic)      {        //neko.Lib.print( err );      }    }    stream.close();━━━━━━━━━━Jack━━━━━━━━━2012.8.20━━━━━━━━━━一次输出：        var stream = sys.io.File.read( "../src/Main.hx", false );           var str = stream.readAll();            stream.close();        Lib.println(str);	更简单：        var str = sys.io.File.getContent( "../src/Main.hx");        Lib.println(str);读网站上的文件：                var string:String = haxe.Http.requestUrl("");                Lib.println(string);━━━━━━━━━━Jack━━━━━━━━━2012.8.21━━━━━━━━━━写入一个新文件：var stream = neko.io.File.write( "jack.txt", false ); 写入一个已存在的文件：var stream = neko.io.File.append( "jack.txt", flase); 写入一个字符串：                var stream = neko.io.File.append( "jack.txt", false );                 var str = "云中君@2012.8.21";                    try                {                        stream.writeString(str);                        stream.flush();                  }                catch ( err : Dynamic)                {                        //neko.Lib.print( err );                }                stream.close();                var str = sys.io.File.getContent( "jack.txt");                Lib.println(str);━━━━━━━━━━Jack━━━━━━━━━2012.8.21━━━━━━━━━━路径和文件是分开的，先创建路径后才能创建文件，参数只能是一个字符串，不能是字符串加其他值的组合，害我改了一晚上：        private static function getStream(path:String,file:String):FileOutput        {                //有路径否                if (!FileSystem.exists(path))                {                        FileSystem.createDirectory(path);                }                //有文件否                if ( FileSystem.exists( file ) )                {                        return File.append( file, false );                }                else                {                        return File.write( file, false );                }        }生成日志文件：        public static function log( logStr:String, ?bold:Bool )        {                var pathStr:String = "log/" + Date.now().getFullYear();                var fileName:String = pathStr + "/log" + Date.now().getFullYear() + Date.now().getMonth() + Date.now().getDate() + ".txt";                                //取stream                var stream = getStream(pathStr,fileName);                //日志                logStr=Date.now().getHours()+":"+ Date.now().getMinutes()+":"+Date.now().getSeconds()+" "+logStr+"\n";                //加分割符                if ( bold )                {                        logStr = "━━━━━━━━━━Log━━━━━━━━━Jack━━━━━━━━━━\n" + logStr + "━━━━━━━━━━end━━━━━━━━━Jack━━━━━━━━━━\n";                }                                //写之                stream.writeString(logStr);                stream.flush();                stream.close();        }