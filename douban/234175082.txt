XML操作

首先当然是加载解析了：var phoneXML = Xml.parse(sys.io.File.getContent("phone.xml"));attributes(),elements()得到的是数组。firstElement()取的是第一个子元素，更实用。firstChild()取的是第一项。xml.exists(att:String)判断是否有att这个属性。xml.get(att:String)取得att这个属性的值。xml.set(att:String,str:String)修改att属性的值为str。nodeName(getNodeName,setNodeName):String括号中不是参数，第一个是得到的值，第二个是可以赋值。parent(getParent,null)表示不可以赋值。━━━━━━━━━━Jack━━━━━━━━━2012.8.25━━━━━━━━━━创建document，书上写错了，应该这样写：        var doc = Xml.createDocument();        #if (flash9 || js || neko)                doc.addChild(Xml.createProlog('xml version="1.0"'));        #end            var root = Xml.createElement("people");          doc.addChild(root);            var p = Xml.createElement("person");           root.addChild(p);           p.set("name", "云中君");           p.addChild(Xml.createPCData("经史子集"));           Lib.println(doc.toString());